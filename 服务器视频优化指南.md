# 服务器视频优化脚本运行指南

## 🎯 概述
由于您的视频文件都在服务器上，我们需要在服务器端运行视频优化脚本来压缩视频并生成多分辨率版本。

## 📋 运行步骤

### 步骤1：上传项目文件到服务器
```bash
# 将整个 VisonProBackend 目录上传到服务器
# 确保包含以下文件：
# - optimize-videos-advanced.js
# - package.json
# - server.js
```

### 步骤2：连接到服务器
```bash
# 使用SSH连接到您的服务器
ssh root@114.55.73.26
```

### 步骤3：安装必要软件

#### 安装 Node.js (如果未安装)
```bash
# CentOS/RHEL
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs

# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装
node --version
npm --version
```

#### 安装 FFmpeg (必需)
```bash
# CentOS/RHEL 8+
sudo dnf install epel-release
sudo dnf install ffmpeg

# CentOS/RHEL 7
sudo yum install epel-release
sudo yum install ffmpeg

# Ubuntu/Debian
sudo apt update
sudo apt install ffmpeg

# 验证安装
ffmpeg -version
```

### 步骤4：配置项目

#### 进入项目目录
```bash
cd /path/to/VisonProBackend  # 替换为您的实际路径
```

#### 安装项目依赖
```bash
npm install
```

#### 检查视频文件路径
```bash
# 查看当前目录结构
ls -la

# 检查视频文件是否在正确位置
ls -la PublicAssets/video/
```

### 步骤5：运行优化脚本

#### 方法1：直接运行
```bash
# 进入项目目录
cd /path/to/VisonProBackend

# 运行脚本
node optimize-videos-advanced.js
```

#### 方法2：后台运行（推荐）
```bash
# 使用 nohup 在后台运行，避免SSH断开时中断
nohup node optimize-videos-advanced.js > video-optimization.log 2>&1 &

# 查看进程
ps aux | grep optimize-videos

# 实时查看日志
tail -f video-optimization.log
```

#### 方法3：使用 screen 运行
```bash
# 安装 screen (如果未安装)
sudo yum install screen  # CentOS/RHEL
sudo apt install screen  # Ubuntu/Debian

# 创建新的 screen 会话
screen -S video-optimize

# 在 screen 中运行脚本
node optimize-videos-advanced.js

# 分离 screen 会话 (按 Ctrl+A 然后按 D)
# 重新连接会话
screen -r video-optimize
```

## 📊 监控脚本运行

### 查看进度
```bash
# 查看日志文件
tail -f video-optimization.log

# 查看CPU和内存使用情况
top
htop  # 如果已安装

# 查看磁盘使用情况
df -h
du -sh PublicAssets/optimized/
```

### 预期输出示例
```
📁 创建目录: /path/to/VisonProBackend/PublicAssets/optimized
📁 创建目录: /path/to/VisonProBackend/PublicAssets/thumbnails

📂 处理分类: activity
📹 找到 15 个视频文件

📹 处理视频: activity1.mp4
📊 原始信息: 1920x1080, 156.23MB
🖼️  生成缩略图: activity1.jpg
🔄 压缩视频: activity1.mp4 -> 1920x1080
✅ 压缩完成: 节省 45.2% 空间
🔄 压缩视频: activity1.mp4 -> 1280x720
✅ 压缩完成: 节省 67.8% 空间
```

## ⚠️ 重要注意事项

### 磁盘空间
- 脚本会生成多个版本的视频，确保有足够磁盘空间
- 建议至少预留原始视频文件总大小的2-3倍空间

### 处理时间
- 视频压缩是CPU密集型任务，处理时间取决于：
  - 视频文件大小和数量
  - 服务器CPU性能
  - 预计每GB视频需要10-30分钟

### 服务器性能影响
- 脚本运行期间会占用较多CPU资源
- 建议在业务低峰期运行
- 可以考虑调整脚本中的并发处理数量

## 🔧 故障排查

### 常见问题

#### FFmpeg 未找到
```bash
# 错误信息：ffmpeg: command not found
# 解决方案：重新安装 FFmpeg
sudo yum install ffmpeg  # CentOS
sudo apt install ffmpeg  # Ubuntu
```

#### 权限问题
```bash
# 错误信息：Permission denied
# 解决方案：检查文件权限
chmod +x optimize-videos-advanced.js
chown -R $(whoami):$(whoami) PublicAssets/
```

#### 内存不足
```bash
# 错误信息：JavaScript heap out of memory
# 解决方案：增加 Node.js 内存限制
node --max-old-space-size=4096 optimize-videos-advanced.js
```

### 脚本中断后恢复
脚本会自动跳过已处理的文件，可以安全重新运行：
```bash
node optimize-videos-advanced.js
```

## 📈 优化建议

### 批量处理
如果视频文件很多，可以分批处理：
```bash
# 修改脚本，只处理特定分类
# 或者手动移动部分视频到临时目录处理
```

### 服务器资源监控
```bash
# 实时监控资源使用
watch -n 1 'free -h && df -h'
```

## ✅ 完成后检查

### 验证输出文件
```bash
# 检查优化后的视频
ls -la PublicAssets/optimized/
ls -la PublicAssets/thumbnails/

# 检查文件大小对比
du -sh PublicAssets/video/
du -sh PublicAssets/optimized/
```

### 更新服务器配置
优化完成后，记得更新 server.js 中的静态文件路径，使其能够提供优化后的视频文件。 
 