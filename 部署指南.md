# 🚀 视频播放卡顿优化 - 部署指南

## 📋 部署前准备

### 1. 确认当前配置
您的nginx配置结构：
- 配置文件：`/etc/nginx/sites-available/xiangbai.conf` ✅
- 启用状态：`/etc/nginx/sites-enabled/xiangbai.conf` ✅（软链接）

### 2. 准备优化文件
需要上传到服务器的文件：
- `nginx-optimized.conf` - 优化后的Nginx配置
- `optimize-videos-streaming.js` - 流媒体视频压缩脚本
- `fix-video-performance.sh` - 一键优化脚本

## 🎯 部署步骤

### 方法一：自动化部署（推荐）

```bash
# 1. 上传优化文件到服务器后端目录
# 假设您的后端目录在 /root/backend 或 /opt/videoprobackend

# 2. 进入后端目录
cd /path/to/your/backend

# 3. 确认优化文件已上传
ls -la nginx-optimized.conf optimize-videos-streaming.js fix-video-performance.sh

# 4. 给脚本执行权限
chmod +x fix-video-performance.sh

# 5. 运行一键优化脚本
./fix-video-performance.sh
```

### 方法二：手动部署（如果自动脚本失败）

#### 步骤1：备份并更新Nginx配置
```bash
# 备份当前配置
sudo cp /etc/nginx/sites-available/xiangbai.conf /etc/nginx/sites-available/xiangbai.conf.backup.$(date +%Y%m%d_%H%M%S)

# 应用新配置
sudo cp nginx-optimized.conf /etc/nginx/sites-available/xiangbai.conf

# 测试配置
sudo nginx -t

# 如果测试通过，重载nginx
sudo systemctl reload nginx
```

#### 步骤2：优化系统网络参数
```bash
# 创建网络优化配置
sudo tee /etc/sysctl.d/99-video-streaming.conf > /dev/null <<EOF
# 网络优化参数 - 视频流播放
net.core.rmem_default = 262144
net.core.rmem_max = 16777216
net.core.wmem_default = 262144
net.core.wmem_max = 16777216
net.core.netdev_max_backlog = 5000
net.ipv4.tcp_rmem = 4096 87380 16777216
net.ipv4.tcp_wmem = 4096 65536 16777216
net.ipv4.tcp_congestion_control = bbr
net.ipv4.tcp_slow_start_after_idle = 0
net.ipv4.tcp_no_metrics_save = 1
EOF

# 应用配置
sudo sysctl -p /etc/sysctl.d/99-video-streaming.conf
```

#### 步骤3：设置视频目录权限
```bash
# 设置正确的权限，让nginx能直接访问视频文件
sudo chown -R www-data:www-data /data/media
sudo chmod -R 755 /data/media
```

#### 步骤4：运行视频优化脚本
```bash
# 确保在包含optimize-videos-streaming.js的目录中
node optimize-videos-streaming.js
```

#### 步骤5：重启后端服务
```bash
# 如果使用PM2
pm2 restart all

# 或者手动重启Node.js服务
# ps aux | grep node  # 找到Node.js进程
# kill <进程ID>
# 然后重新启动您的Node.js应用
```

## 🔍 验证优化效果

### 1. 检查Nginx状态
```bash
sudo systemctl status nginx
sudo nginx -t
```

### 2. 检查视频文件是否可直接访问
```bash
# 测试nginx是否能直接提供视频文件
curl -I http://localhost/assets/video/activity/your-video-file.mp4
# 应该看到200状态码和Accept-Ranges: bytes
```

### 3. 检查优化后的视频文件
```bash
ls -la /data/media/optimized/
# 应该看到按分类生成的240p、360p、480p版本
```

### 4. 测试API响应
```bash
curl http://localhost:3000/api/video-categories
# 应该返回JSON格式的视频分类数据
```

## 🎯 关键优化点说明

### Nginx优化
- ✅ **启用sendfile零拷贝传输**：大幅提升文件传输效率
- ✅ **视频文件直接由Nginx提供**：绕过Node.js，减少延迟
- ✅ **优化Range请求支持**：支持视频拖拽和续播
- ✅ **智能分片传输**：512KB分片，适合网络传输

### 视频压缩优化
- ✅ **GOP设置为60帧**：2秒一个关键帧，便于网络传输
- ✅ **启用faststart**：视频元数据前置，加快播放开始
- ✅ **稳定码率控制**：避免码率波动导致的卡顿
- ✅ **多分辨率版本**：240p/360p/480p自适应

## 🚨 故障排除

### 如果Nginx配置测试失败
```bash
# 查看错误详情
sudo nginx -t

# 恢复备份配置
sudo cp /etc/nginx/sites-available/xiangbai.conf.backup.* /etc/nginx/sites-available/xiangbai.conf
sudo systemctl reload nginx
```

### 如果视频仍然卡顿
1. **检查服务器资源**：
   ```bash
   htop          # CPU和内存使用情况
   iostat -x 1   # 磁盘I/O性能
   iftop         # 网络带宽使用
   ```

2. **检查网络延迟**：
   ```bash
   ping xiangbai.cc  # 从您的本地网络ping服务器
   ```

3. **检查带宽**：
   - 确保服务器出口带宽 ≥ 10Mbps
   - 可以考虑升级带宽或使用CDN

### 如果优化脚本执行失败
- 确保以非root用户身份运行脚本
- 脚本会在需要时请求sudo权限
- 检查所有优化文件是否在当前目录

## 📊 预期效果

优化完成后，您应该看到：

- **🚀 视频加载速度提升60-80%**
- **📱 大幅减少卡顿和缓冲**
- **💾 服务器CPU使用率降低30-50%** 
- **🌐 更好的网络适应性**
- **⚡ 视频拖拽更流畅**

## 📞 支持

如果在部署过程中遇到任何问题，请提供：
1. 错误日志输出
2. 当前nginx配置
3. 服务器资源使用情况
4. 网络测试结果

这样我可以为您提供更精确的解决方案。 