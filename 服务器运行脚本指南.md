# 服务器视频优化脚本运行指南

## 🚀 快速运行步骤

### 1. 上传项目到服务器
将 VisonProBackend 文件夹上传到服务器

### 2. 连接服务器并安装依赖
```bash
# SSH连接服务器
ssh root@114.55.73.26

# 进入项目目录
cd /path/to/VisonProBackend

# 安装FFmpeg (必需)
sudo yum install epel-release ffmpeg  # CentOS
# 或
sudo apt install ffmpeg  # Ubuntu

# 安装Node.js依赖
npm install
```

### 3. 运行优化脚本
```bash
# 后台运行脚本（推荐）
nohup node optimize-videos-advanced.js > optimization.log 2>&1 &

# 查看运行日志
tail -f optimization.log

# 查看进程状态
ps aux | grep optimize
```

## 📊 预期结果
- 生成 /data/media/optimized/ 目录存放压缩视频
- 生成 /data/media/thumbnails/ 目录存放缩略图
- 每个视频生成多个分辨率版本（480p, 720p, 1080p）
- 通过以下URL访问：
  - 优化视频：http://114.55.73.26:3001/assets/optimized/分类/文件名
  - 缩略图：http://114.55.73.26:3001/assets/thumbnails/分类/文件名.jpg

## ⚠️ 注意事项
- 确保磁盘空间充足（至少是原视频2-3倍大小）
- 处理时间较长，建议后台运行
- 可随时中断，重新运行会跳过已处理文件 